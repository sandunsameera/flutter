import 'package:flutter/material.dart';

class CodeSearch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Code Search"),
        actions: <Widget>[
        ],
      ),
    );
  }
}
